/* Form.css */

/* Background container */
.form-container {
    background: linear-gradient(to right, #6a11cb, #2575fc);
    color: white;
    padding: 2rem;
    height: 100vh;
  }
  
  /* Form card */
  .form-card {
    background: white;
    padding: 2rem;
    border-radius: 8px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    color: black;
  }
  
  /* Form header */
  .form-header {
    text-align: center;
    text-transform: uppercase;
    font-weight: bold;
    margin-bottom: 1.5rem;
  }
  
  /* Labels */
  .form-label {
    font-weight: bold;
  }
  
  /* Inputs */
  .form-control,
  .form-select {
    border: 2px solid #0d6efd; /* Primary Bootstrap color */
    border-radius: 4px;
  }
  
  /* Input group prefix */
  .input-group-text {
    background-color: #0d6efd;
    color: white;
    border-radius: 4px 0 0 4px;
  }
  
  /* Submit button */
  .btn-submit {
    background-color: #0d6efd;
    color: white;
    font-weight: bold;
    border: none;
    transition: background-color 0.3s ease;
  }
  
  .btn-submit:hover {
    background-color: #0b5ed7; /* Darker shade of Bootstrap primary color */
  }
  
  /* Checkbox */
  .form-check-input {
    border: 2px solid #0d6efd;
  }
  
  